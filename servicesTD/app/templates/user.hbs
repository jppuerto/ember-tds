<ul class="list-group">
  <li class="list-group-active">{{plural model.countActive}}</li>
  {{#each model.services as |service|}}
    <li {{action "toggleActive" service}}></li>
    <li class="list-group-item {{if service.active 'active' ''}}" {{ action "toggleActive" service}}>
      {{service.name}} <span>{{format-currency service.price}} </span>
    </li>
  {{/each}}
</ul>
total : {{model.sumActive}}
€
<br>

{{input value=model.codepromo class="form-control" placeholder='Entrez votre code promo '}}

{{#if model.isRemise}}
  Remise : {{ model.sumRemise }}
  <br>
  Total avec remise : {{ model.sumActiveRemise}}
{{/if}}
{{outlet}}